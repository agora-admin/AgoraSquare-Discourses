apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "agora-discourses-chart.fullname" . }}-test-connection"
  labels:
    {{- include "agora-discourses-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "agora-discourses-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
